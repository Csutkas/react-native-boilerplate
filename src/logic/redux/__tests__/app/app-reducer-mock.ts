import {IAppReducer} from '../../app/app-models';

export const AppState: IAppReducer = {
  loading: false,  
};

describe('empty test', () => {
  it('empty test', () => {
    expect(true).toEqual(true);
  });
});
